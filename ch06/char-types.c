#include <stdio.h>

int main (void) {
    char a = 'A';
    char b = a + 1;


    printf("The character (a) %c.\n", a);
    printf("The integer (a) %d.\n", a);

    printf("The character (b) %c.\n", b);
    printf("The integer (b) %d.\n", b);

    return 0;
}